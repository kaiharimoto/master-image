<UserControl x:Class="MasterImage.App.Views.CompareView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:MasterImage.App.Views">
    <Grid Background="Black">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- The border is the active-pane marker. It's on the outside of each pane rather than
             drawn over the photo so it can never hide the detail you're comparing. -->
        <Border x:Name="LeftPaneBorder" Grid.Column="0"
                BorderThickness="2" BorderBrush="Transparent" Margin="0,0,1,0">
            <local:SingleImageView x:Name="LeftPane" />
        </Border>
        <Border x:Name="RightPaneBorder" Grid.Column="1"
                BorderThickness="2" BorderBrush="Transparent" Margin="1,0,0,0">
            <local:SingleImageView x:Name="RightPane" />
        </Border>
    </Grid>
</UserControl>
