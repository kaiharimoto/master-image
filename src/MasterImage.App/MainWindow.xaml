<Window x:Class="MasterImage.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:local="clr-namespace:MasterImage.App.Views"
        Title="Master Image"
        Background="Black"
        WindowStyle="None"
        ResizeMode="CanResize"
        Width="1200" Height="800">
    <!-- Keeps the borderless, chrome-free look but restores real window behaviour: an 8px grab
         zone on every edge and corner (the default borderless border is ~4px and genuinely hard
         to hit). CaptionHeight is 0 deliberately — a caption strip would swallow clicks along the
         top of the photo; dragging the image itself moves the window instead (see SingleImageView). -->
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome ResizeBorderThickness="8"
                            CaptionHeight="0"
                            GlassFrameThickness="0"
                            CornerRadius="0"
                            UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </Window.Resources>
    <Grid x:Name="RootGrid">
        <ContentControl x:Name="SingleImageHost">
            <local:SingleImageView x:Name="SingleImageViewControl" />
        </ContentControl>
        <ContentControl x:Name="CompareHost" Visibility="Collapsed">
            <local:CompareView x:Name="CompareViewControl" />
        </ContentControl>
        <ContentControl x:Name="GridHost" Visibility="Collapsed">
            <local:TileGridView x:Name="TileGridViewControl" />
        </ContentControl>
        <ContentControl x:Name="NavigationOverlayHost">
            <local:NavigationOverlay x:Name="NavigationOverlayControl" />
        </ContentControl>
        <ContentControl x:Name="ShortcutsOverlayHost"
                        Visibility="{Binding IsShortcutsOverlayVisible, Converter={StaticResource BoolToVis}}">
            <local:ShortcutsOverlay />
        </ContentControl>
    </Grid>
</Window>
